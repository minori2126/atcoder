#include<bits/stdc++.h>
using namespace std;

int main(){
	int T;
	cin >> T;
	for(int i=0;i<T;i++){
		int N;
		cin >> N;
		vector<int> S(N);
		for(int i=0;i<N;i++){
			cin >> S[i];
		}
		sort(v.begin() + 1, v.end() - 1);

	return 0;
}
